% Run all the detection fits from cached data

% Detection.  You might want to run these one at at time,
% so that you can see the printout at the end of each one.
fitDetectionCachedData('MAB');
fitDetectionCachedData('BMC');
fitDetectionCachedData('KAS');

